# 当前计分方式与计算公式

本文档概括当前代码中的实际计分逻辑，以 `server.js` 的提交与报告计算实现为准。

## 1. 符号定义

对第 $i$ 道题，定义：

- $C_i$：标准情绪集合，元素数量为 $c_i=|C_i|$。
- $A_i$：用户选择的情绪集合，元素数量为 $a_i=|A_i|$。
- $h_i=|A_i\cap C_i|$：用户选中且属于标准答案的情绪数量。
- $s_{ij}\in\{1,2,3,4,5\}$：第 $j$ 个标准情绪的标准强度。
- $u_{ij}\in\{1,2,3,4,5\}$：用户对该标准情绪给出的强度；如果用户没有选择该标准情绪，则视为未匹配。
- $w_i$：题目分值，即创建答卷时保存的 `points_snapshot`。
- $L_i$：该题的标签识别得分率，范围为 0–100。
- $S_i$：该题的强度判断得分率，范围为 0–100。
- $T_i$：该题的综合得分率，范围为 0–100。

题目的标准答案、标准强度、题目分值、题型、模态等信息会在创建答卷时保存为快照。之后修改题库不会改变已有答卷的计算依据。

## 2. 标签识别得分

### 2.1 单标准答案题

当标准情绪只有一个，即 $c_i=1$ 时：

$$
L_i=
\begin{cases}
100, & h_i=1 \\
0, & h_i=0
\end{cases}
$$

也就是说，标准情绪选中则标签得 100 分，否则得 0 分。单选题接口限制用户只能选择一个情绪。

### 2.2 多标准答案题

当标准情绪多于一个，即 $c_i>1$ 时：

$$
L_i=
\begin{cases}
100\times\dfrac{2h_i}{a_i+c_i}, & h_i>0 \\
0, & h_i=0
\end{cases}
$$

该公式等价于集合匹配的 $\text{Dice}/\text{F1}$ 形式，同时考虑：

- 漏选：会使 $h_i$ 相对 $c_i$ 变小；
- 多选错误情绪：会增大 $a_i$；
- 完全正确：当 $A_i=C_i$ 时，$L_i=100$。

例如，标准答案为 3 个情绪，用户选择 4 个，其中命中 2 个：

$$
L_i=100\times\frac{2\times2}{4+3}\approx57.14
$$

## 3. 强度判断得分

系统分别检查每一个标准情绪。对第 $j$ 个标准情绪，其强度得分为：

$$
I_{ij}=
\begin{cases}
100\times\max\left(0,1-\dfrac{|u_{ij}-s_{ij}|}{4}\right), & \text{用户选中了该标准情绪} \\
0, & \text{用户未选中该标准情绪}
\end{cases}
$$

因为用户强度和标准强度都在 1–5 之间，实际对应关系为：

| 强度绝对误差 $|u_{ij}-s_{ij}|$ | 得分率 |
| ---: | ---: |
| 0 | 100 |
| 1 | 75 |
| 2 | 50 |
| 3 | 25 |
| 4 | 0 |

一道题的强度判断得分，是所有标准情绪强度得分的算术平均：

$$
S_i=\frac{1}{c_i}\sum_{j=1}^{c_i}I_{ij}
$$

需要特别注意：

- 漏选某个标准情绪时，该标准情绪的强度得分为 0。
- 用户额外选择的错误情绪不直接参与强度平均，但会通过标签识别公式降低 $L_i$。
- 强度必须与用户所选情绪逐一对应。

## 4. 单题综合得分

每题由标签识别和强度判断两部分组成：

$$
T_i=0.6L_i+0.4S_i
$$

即：

- 标签识别占 60%；
- 强度判断占 40%。

这里的 $T_i$ 是百分制得分率。该题实际获得的原始分值为：

$$
P_i=w_i\times\frac{T_i}{100}
$$

例如，一道题权重为 20 分，标签得分率为 100，强度得分率为 75：

$$
T_i=0.6\times100+0.4\times75=90
$$

$$
P_i=20\times\frac{90}{100}=18
$$

## 5. 整份答卷得分

假设答卷共有 $n$ 道题。

### 5.1 满分和实际得分

答卷满分：

$$
P_{\max}=\sum_{i=1}^{n}w_i
$$

实际获得分数：

$$
P_{\text{earned}}=\sum_{i=1}^{n}w_i\frac{T_i}{100}
$$

报告中的 `max_points` 和 `earned_points` 会保留两位小数。

### 5.2 综合得分率

$$
T_{\text{overall}}=
\frac{\sum_{i=1}^{n}w_iT_i}{\sum_{i=1}^{n}w_i}
$$

### 5.3 总标签得分率

$$
L_{\text{overall}}=
\frac{\sum_{i=1}^{n}w_iL_i}{\sum_{i=1}^{n}w_i}
$$

### 5.4 总强度得分率

$$
S_{\text{overall}}=
\frac{\sum_{i=1}^{n}w_iS_i}{\sum_{i=1}^{n}w_i}
$$

上述三个总得分率在报告中均四舍五入到一位小数。由于每题满足 $T_i=0.6L_i+0.4S_i$，因此在舍入误差之外，整体也满足：

$$
T_{\text{overall}}=0.6L_{\text{overall}}+0.4S_{\text{overall}}
$$

## 6. 分维度得分

对于任意题目子集 $G$，分组得分率采用同样的题目分值加权公式：

$$
T_G=\frac{\sum_{i\in G}w_iT_i}{\sum_{i\in G}w_i}
$$

当前报告按以下维度分组：

- 题目类型：情绪识别 `recognition`、情绪推理 `reasoning`；
- 素材模态：图像、文本、音频、视频；
- 情绪类别：题目配置的 `emotion_category`。

各分组得分率四舍五入到一位小数。如果某个分组没有题目，则题型指标返回 `null`；没有出现的模态或情绪类别通常不会生成对应键。

### 情绪识别子项

报告还会仅针对 `recognition` 题目计算：

$$
L_{\text{recognition}}=
\frac{\sum_{i\in R}w_iL_i}{\sum_{i\in R}w_i}
$$

$$
S_{\text{recognition}}=
\frac{\sum_{i\in R}w_iS_i}{\sum_{i\in R}w_i}
$$

其中 $R$ 为所有情绪识别题的集合。当前没有单独输出情绪推理题的标签和强度子项。

## 7. 作答完整性与特殊情况

- 当前前后端都要求所有题目完成后才能提交；没有答案或标记为跳过的题目会阻止提交。
- 评分代码仍有防御性逻辑：如果某题没有有效回答或被跳过，则该题的 $L_i$、$S_i$ 和 $T_i$ 都为 0。
- 单选题只能选 1 个情绪；多选题允许选择 1–5 个互不重复的情绪。
- 所选情绪必须来自该答卷题目快照中的候选项，强度必须为 1–5 的整数。
- “是否看过该影视作品”、作答时间和修改次数不参与得分。
- 修改题库中的标准答案、强度或分值，不会重新计算或改变已经创建的答卷，因为计算使用答卷快照。
- 已完成的答卷不可重复提交。

## 8. 时间和管理端统计

单题有效作答时间来自前端记录的页面可见且窗口获得焦点期间的累计时间。整份答卷的统计为：

$$
D_{\text{total}}=\sum_{i=1}^{n}d_i
$$

$$
D_{\text{average}}=\operatorname{round}\left(\frac{D_{\text{total}}}{n}\right)
$$

其中时间单位均为毫秒。时间不参与任何得分计算。

管理后台仪表盘的“平均得分”是所有已完成答卷 `total_score` 的普通算术平均值，并在 SQL 中四舍五入到一位小数；它不会按各答卷满分再次加权。

## 9. 前端展示等级

报告页根据综合得分率显示文字等级，但该等级只用于展示，不改变任何分数：

| 综合得分率 | 展示等级 |
| ---: | --- |
| $T_{\text{overall}}\ge85$ | 优秀 |
| $70\le T_{\text{overall}}<85$ | 良好 |
| $60\le T_{\text{overall}}<70$ | 尚可 |
| $T_{\text{overall}}<60$ | 需提升 |

## 10. 完整计算示例

某道多选题的配置与作答如下：

- 标准情绪：快乐（强度 5）、惊喜（强度 4）、感动（强度 3）；
- 用户选择：快乐（强度 4）、惊喜（强度 2）、紧张（强度 5）；
- 题目分值：$w_i=20$。

此时 $c_i=3$、$a_i=3$、$h_i=2$，所以：

$$
L_i=100\times\frac{2\times2}{3+3}=66.67
$$

三个标准情绪对应的强度得分分别为：

- 快乐：$100\times(1-|4-5|/4)=75$；
- 惊喜：$100\times(1-|2-4|/4)=50$；
- 感动：未选择，得 0。

因此：

$$
S_i=\frac{75+50+0}{3}=41.67
$$

$$
T_i=0.6\times66.67+0.4\times41.67\approx56.67
$$

$$
P_i=20\times\frac{56.67}{100}\approx11.33
$$

系统内部使用未提前舍入的浮点结果继续汇总，最终再按各报告字段的规则进行四舍五入，因此手工逐步舍入时可能出现极小差异。
