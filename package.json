{
  "name": "zhijing-emotion-assessment",
  "version": "1.0.0",
  "private": true,
  "description": "多模态情绪感知测评平台",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js && node --check scripts/update-question-points.js",
    "questions:update-points": "node scripts/update-question-points.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^2.0.2"
  }
}
